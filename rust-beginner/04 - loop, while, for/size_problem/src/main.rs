fn main() {
    let vector: Vec<i32> = vec![1, 2, 3, 4, 5];
    let sum: i32 = vector[0] 
                 + vector[1]
                 + vector[2]
                 + vector[3]
                 + vector[4];
    println!("The sum of the first 5 elements: {}", sum);
}
