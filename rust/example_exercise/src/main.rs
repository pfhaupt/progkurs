// Rating 1/3 - Yellow
fn main() {
    let a = 0; // What type does `a` have?
    let b = 5; // What type does `b` have?
    let c: u32 = a + b;
    println!("the v{a}lue of c is: {}", c);
    // What's the output of the program?
}

